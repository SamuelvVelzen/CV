import React, { ReactElement } from 'react';

import './../../styles/style/modules/bannerText.scss';

function BannerText(): ReactElement {
    return (
        <section id={'bannerText'} className={'text-light'}>
            <span>test</span>
        </section>
    );
}

export default BannerText;
